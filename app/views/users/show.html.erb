<div class="users-new-wrapper">
  <div class="bkRGBA">
    <div class="container">
      <div class="row">
        <div class="col-md-offset-4 col-md-4 users-new-container">
            <h1>Mypage</h1>
            <% if @user.image? %>
             <%= image_tag @user.image ,class="picture" %>
            <% else %>
             <%= image_tag("/images/icon/icon-mama-02-160px.png") %>
            <% end %>
           <div class="col-md-8">
            <p><span class="text"><%= @user.name %></span>
            &nbsp;<%= @user.live %>
            <% if @user.age.present? %> &nbsp;<%= @user.age %>代 <% end %>
            </p>
            <p class="text">自己紹介</p>
            <p><%= @user.intro %></p>
           </div>
      </div>
    </div>
   </div>
  </div>
</div>